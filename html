<!DOCTYPE html>
<html>
<head>
    <title>LLM Note-Taking Tool</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h1 class="mt-5">LLM Note-Taking Tool</h1>
        <form method="post" class="mt-3">
            <div class="form-group">
                <textarea class="form-control" name="note" rows="4" placeholder="Write your note here..."></textarea>
            </div>
            <button type="submit" class="btn btn-primary">Add Note</button>
        </form>
        <h2 class="mt-5">Notes:</h2>
        <ul class="list-group">
            {% for note in notes %}
                <li class="list-group-item">{{ note[1] }}</li>
            {% endfor %}
        </ul>
        <h2 class="mt-5">Ask LLM:</h2>
        <form method="post" action="/ask" class="mt-3">
            <div class="form-group">
                <input type="text" class="form-control" name="question" placeholder="Ask a question about your notes">
            </div>
            <button type="submit" class="btn btn-primary">Ask</button>
        </form>
        {% if answer %}
        <h3 class="mt-5">Answer:</h3>
        <p>{{ answer }}</p>
        {% endif %}
    </div>
</body>
</html>
